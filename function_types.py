def list_shift(números, valor):
    for i in range(len(números)):
        números[i] += (valor)
def calc_avg(números):
    return sum(números) / len(números)
def print_normalized(lista):
    print(lista)